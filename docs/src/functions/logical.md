---
layout: doc
outline: deep
lang: en-US
---

# Logical functions

Logical functions evaluate conditions and return `TRUE` or `FALSE`. They let you combine several tests (AND, OR), choose between alternatives based on a result (IF), and trap and handle errors.
